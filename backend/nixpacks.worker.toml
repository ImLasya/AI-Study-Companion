[phases.setup]
nixPkgs = ["python311", "gcc", "libstdcxx5", "zlib", "libGL"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -e ."
]

[start]
cmd = "celery -A app.workers.celery_app.celery_app worker --loglevel=info --concurrency=2"
